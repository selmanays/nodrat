# OS
.DS_Store
Thumbs.db
*.swp
*.swo

# IDE
.vscode/
.idea/
*.iml
.cursor/

# Environment
.env
.env.local
.env.*.local
*.pem
*.key

# Secrets (Cloudflare Origin CA, etc.) — VPS'te /opt/nodrat/secrets, repo'da YOK
secrets/

# Secrets (sops-encrypted .env.sops / .env.encrypted repo'da OK — #38)
!*.sops
!infra/.env.encrypted
!infra/.env.encrypted.example
!infra/.sops.yaml

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*

# Node / Next.js
node_modules/
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Docker
docker-compose.override.yml

# Storage volumes (local dev)
postgres_data/
redis_data/
minio_data/
caddy_data/

# Logs
logs/
*.log

# Backup local
backups/local/

# Temp / tmp
tmp/
*.tmp

# Obsidian (wiki/ ikinci beyin için vault — config paylaşılır, workspace/plugin install ignore)
.obsidian/workspace
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache/
# .obsidian/plugins/ — her dev kendi install eder; data.json'lar API key gibi sırlar içerebilir
.obsidian/plugins/

# OS specific
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# Claude (private — kullanıcı kendi ayarları)
.claude/projects/
.claude/cache/
# .claude/skills/ ve .claude/commands/ — repo'da kalır (paylaşılan)

# Nodrat application
apps/api/.venv/
apps/api/.coverage
apps/api/uv.lock
apps/web/.next/
apps/web/.turbo/
*.pyc

# Alembic generated (commit edilmez, schema'ya göre regenerate)
# apps/api/alembic/versions/*.py — commit ediliyor

# Test artifacts
playwright-report/
test-results/
.eval-cache/

# Local Docker volumes (geliştirme)
.docker/

# Backup local
backups/local/

# Wiki Obsidian alt-vault state (kök .obsidian/ yeterli — wiki kendi vault değil)
wiki/.obsidian/
wiki/Başlıksız.canvas

# MCP config — plain text API key içerir, repo'ya gitmemeli
# Yeni dev: cp .mcp.json.example .mcp.json + key gir (bkz. wiki/SETUP.md)
# Sebep: Claude Code'da .mcp.json env var expansion + settings.local.json env
# field MCP server'a aktarılmıyor (anthropic/claude-code#1254). Plain text +
# gitignore tek güvenilir çözüm.
.mcp.json
