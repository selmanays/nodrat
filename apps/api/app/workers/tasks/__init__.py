"""Celery task'lar — namespace.

Modül listesi:
    sources.py        — kaynak crawl + healthcheck (#16)
    articles.py       — article fetch + clean (Faz 1)
    maintenance.py    — backup + cleanup (Faz 1)
    embedding.py      — chunk + embed (Faz 2)
    rag.py            — clustering + agenda (Faz 2)
"""
